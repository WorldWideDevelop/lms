import React from "react";
import { HeroContainer, HeroBg, HeroContent } from "./HeroSection.elements";
const HeroSection = () => {
  return (
    <HeroContainer>
      <HeroBg>
        <HeroContent>Bienvenido a Qori</HeroContent>
      </HeroBg>
    </HeroContainer>
  );
};

export default HeroSection;
